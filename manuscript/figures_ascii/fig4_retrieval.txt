# fig4_retrieval.txt — Module II retrieval pipeline
# Manuscript: Methods, "Clinical Guideline Lookup" subsection.
# Polish target: TikZ horizontal diagram with the graph-expansion box
# rendered as v0-stubbed in a different color (e.g., grayed out).

╔══════════════════════════════════════════════════════════════════════════════╗
║                Module II — retrieval pipeline (v0)                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

  ┌──────────────────────────────────────┐
  │  PatientConcernProfile  (Module I)   │
  └──────────────────────────────────────┘
                  │
                  │ decompose_profile()
                  ▼
  ┌──────────────────────────────────────┐
  │  Sub-queries  (e.g., 7 for p001)     │
  │    • per problem      (3)            │
  │    • per goal         (3)            │
  │    • global context   (1)            │
  └──────────────────────────────────────┘
                  │
                  │ BGE-large-en-v1.5 → 1024-d (L2-normalized)
                  ▼
  ┌──────────────────────────────────────┐
  │  Query embeddings (7 vectors)        │
  └──────────────────────────────────────┘
                  │
                  │ Chroma ANN search · top_k = 20 per query · cosine
                  ▼
  ┌──────────────────────────────────────────────────────────────────┐
  │  Seed chunks (top-k per sub-query, deduped)                      │
  │     ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐   . . .      │
  │     │ c-1 │   │ c-2 │   │ c-3 │   │ c-4 │   │ c-5 │              │
  │     └─────┘   └─────┘   └─────┘   └─────┘   └─────┘              │
  └──────────────────────────────────────────────────────────────────┘
                  │
                  │ expand_neighborhood(seeds)
                  │   ── v0: STUBBED — flat retrieval; returns seeds ──
                  │
                  │  Planned edge legend (v1+):
                  │       cites              (forward + back)
                  │       supersedes         (newest among same DOI fam.)
                  │       shares-descriptor  (top MeSH overlap)
                  ▼
  ┌──────────────────────────────────────────────────────────────────┐
  │  Expanded set (v0 == seed set)                                   │
  │     ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ...  │
  │     │ c-1 │ │ c-2 │ │ c-3 │ │ c-4 │ │ c-5 │ │ c-6 │ │ c-7 │      │
  │     └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘ └─────┘      │
  └──────────────────────────────────────────────────────────────────┘
                  │
                  │ aggregate to document level (group by pmid)
                  ▼
  ┌──────────────────────────────────────┐
  │  Candidate documents (~80 for p001)  │
  └──────────────────────────────────────┘
                  │
                  │ rerank — greedy MMR with weights
                  │   total = 0.5·semantic_similarity
                  │         + 0.2·recency       (linear over 2018–2026)
                  │         + 0.2·authority     (30-journal lookup)
                  │         + 0.1·coverage_bonus (uncovered sub-queries)
                  ▼
  ┌──────────────────────────────────────┐
  │  RankedDocumentSet — top 20 docs     │
  │  → input to Module III                │
  └──────────────────────────────────────┘
